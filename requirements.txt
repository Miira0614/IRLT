# ============================================
# 半导体需求追溯链项目依赖
# ============================================

# 核心依赖
openai>=1.0.0          # LLM API调用（支持SiliconFlow和本地模型）
openpyxl>=3.1.0        # Excel文件读写（生成预览表格）
tqdm>=4.65.0           # 进度条显示

# FastAPI 后端依赖
fastapi>=0.100.0       # FastAPI Web框架
uvicorn>=0.23.0        # ASGI服务器
pydantic>=2.0.0        # 数据验证（FastAPI依赖）

# 可选依赖（根据需要安装）
# python-dotenv>=1.0.0  # 环境变量管理（如需从.env文件加载配置）
# requests>=2.31.0      # HTTP请求（如需扩展API调用）
